<div class="container mt-5">
	<% @articles.each do |article| %>
		<div class="card mb-5">
			<div class="card-title">
				<%= article.title %>
			</div>
			<div class="card-body">
				<%= article.body %>
			</div>
		</div>
	<%end %>
</div>