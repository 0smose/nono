<%= render 'admins/dashboard' %>
<div class="container margin-footer mt-5 ">
		<%= link_to '+', new_admins_catalogue_path, class: ' btn btn-primary mb-5', style: "font-size: 30px;" %>
	<% @catalogues.each do |cat| %>
		<div class="card mb-5 ">
			<div class="card-body">
			<div class="card-title text-center ">
				<%= cat.name %>
			</div>			
			<div class="row">
				
			<% cat.pictures.each do |picture| %>
			<div class="col col-3 mb-3">
			<%= image_tag(picture, class:"fit my-scroll") %>
			</div>
		<% end %>
		
		</div>
		
		 <div class="my-btn-display mb-5 mt-5">
			<%= link_to "Éditer", edit_admins_catalogue_path(cat.id), class: "btn my-btn btn-info " %>
			<%= link_to "Supprimer", admins_catalogue_path(cat.id), method: :delete, data: {confirm: "are you sure ?"}, class: "btn my-btn btn-danger  " %>
			</div>
			</div>
		</div>	
	<%end %>
</div>